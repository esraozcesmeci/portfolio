{
  "name": "link-directory",
  "version": "1.0.0",
  "description": "A curated link directory with rich Open Graph previews, deployable as a GitHub Pages static site.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "migrate": "node build.mjs migrate",
    "test": "node --experimental-vm-modules node_modules/.bin/vitest run"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "open-graph-scraper": "^6.8.0"
  },
  "devDependencies": {
    "fast-check": "^3.22.0",
    "vitest": "^2.1.0"
  }
}
